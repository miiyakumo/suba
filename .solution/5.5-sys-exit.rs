//! # sys_exit 实现
//!
//! 参考实现：退出系统调用。

/// 退出系统调用。
///
/// 终止当前任务。
///
/// # 参数
/// - `exit_code`: 退出码
///
/// # 返回值
/// 不返回（在真实内核中，此函数不会返回）。
pub fn sys_exit(exit_code: i32) -> usize {
    // 在真实内核中：
    // 1. 获取当前任务
    // 2. 设置状态为 Exited
    // 3. 记录退出码
    // 4. 触发调度（不再返回）
    //
    // Mock 环境下，我们无法真正获取"当前任务"，
    // 因为没有全局的 current_task 指针。
    // 这个函数在集成测试中会被测试。

    // Mock: 返回退出码（真实内核中不返回）
    exit_code as usize
}
